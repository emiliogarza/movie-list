import { DurationTimeStampPipe } from './duration-time-stamp.pipe';

describe('DurationTimeStampPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationTimeStampPipe();
    expect(pipe).toBeTruthy();
  });
});
